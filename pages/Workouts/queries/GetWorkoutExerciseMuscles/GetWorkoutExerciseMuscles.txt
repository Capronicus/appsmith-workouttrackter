SELECT
    m.name,
    m.body_region,
    em.intensity
FROM exercise_muscles em
JOIN muscles m
    ON m.id = em.muscle_id
WHERE em.exercise_id = {{ tblWorkoutExercises.selectedRow.id }}
ORDER BY
    em.intensity DESC;